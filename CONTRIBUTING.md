# 贡献指南

先使用 Issue 描述问题、触发条件和预期行为，再为明确范围提交 PR。新功能请给出可验证的使用场景；修复应覆盖具体失败路径。

1. 从 main 建立分支，生成 Xcode 工程。
2. 保持计划/实际、重量/次数/秒/米/器械 cal 的数据语义。
3. 修改模型时检查迁移、备份往返、草稿恢复及导入导出。
4. 运行 `python3 scripts/check-repository.py`、相关 Swift 测试及 backend 检查。
5. 如涉及 UI，附使用合成学员的复现步骤或图片；不要附真实个人信息。
6. 在 PR 中说明变化、原因、验证结果和剩余边界。更新对应文档。

禁止提交 API Key、访问令牌、签名身份、原始用户工作簿、真实体测数据、个人截图、数据库或构建日志。gitignore 不能清除已提交内容；提交前检查 Git diff 和暂存文件。密钥怀疑泄露时先撤销/轮换，再清理历史。

默认测试必须不依赖个人文件、生产服务或真实付费请求。使用合成数据、URLProtocol 模拟或显式自愿的集成测试。提交的贡献按项目 MIT 许可提供，且贡献者须有相应权利。

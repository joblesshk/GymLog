# 安全问题

当前维护范围为 main 分支；尚不承诺旧版本的独立安全维护。

不要在公开 Issue、PR、截图或日志中附带密钥、个人健康资料或可直接利用的攻击细节。优先使用仓库 Security 页的私密漏洞报告功能（若可用）；仓库保持私有时，可向具有访问权限的维护者提交不含秘密值的 Issue。若未来公开而私密报告通道未启用，请先用无细节的 Issue 请求私密沟通方式。

报告应包括受影响版本、最小合成复现、攻击前提、影响及缓解建议。无需提供真实令牌。若你自己的密钥已经暴露，请在供应商处撤销/轮换；单纯删除文件或提交不能使旧密钥失效。

源码发行版默认不配置云端服务。Worker 的随机安装身份和配额是轻量机制，不能代替用户认证、设备证明、访问准入、供应商预算或部署平台的滥用防护。不要将示例 Worker 无限制开放后假定费用安全。

CI 使用只读权限和无生产秘密的测试。新增外部 Action 应固定到完整提交 SHA，更新由审核后的依赖 PR 完成。

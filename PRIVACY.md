# 隐私与数据处理

仓库不包含真实学员、病史、体测照片、原始训练工作簿或作者的生产服务配置。示例数据均为合成；正常应用新安装不导入测试学员。

训练、档案与导入资料由本机处理和保存。用户主动导出的备份、CSV、互传文件可能包含个人信息；系统分享及系统设备备份的范围由用户和系统配置决定。这些导出不应提交到代码仓库。

Excel 解析和体测 OCR 使用本机代码及 Apple 框架。云端语音会将音频、转写及必要训练上下文发往配置的 Worker 和 ASR/语言模型服务。AI 评价会发送动作、计划/实际记录、目标、近期训练摘要和估算假设，不主动加入姓名、电话或完整病史；用户写入自由文本的信息仍可能随上下文发送。

供应商密钥只由部署者保存在服务端。客户端将随机安装身份保存在 Keychain，并从 Worker 获取临时操作令牌。安装身份及额度状态可能由 Worker 持久化；不是个人账户，也不是硬件身份认证。

应用代码不自行长期保存语音录音；这不保证云供应商或平台没有日志、缓存或保留策略。部署者必须核对自己的 Cloudflare、ASR 和模型服务条款、日志配置、保留周期及适用地区要求。

用户可在应用中管理自己的记录；卸载是否移除系统备份或 Keychain 数据由 iOS 行为决定，不应将卸载表述为所有副本都已删除。仓库维护者无法远程读取或删除他人自行部署的记录。

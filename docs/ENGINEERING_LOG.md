# 工程记录

## 2026-09-24 — 远端分支历史姓名清理完成

- 经用户明确授权，对 main、设计分支及五个依赖更新分支重写历史，将两处历史源码注释中的真实学员姓名替换为示例标签。推送前逐一核对七个远端旧提交，使用 atomic push 与明确旧 SHA 的 force-with-lease，七个分支全部更新成功。
- 从 GitHub 重新克隆并核对七个分支的提交 SHA；扫描分支可达历史中的 590 个唯一文件版本及提交信息，已知学员身份零匹配。main 当前源码树与清理前一致；本地 main 已同步，本机发布配置和 TestFlight 包未改变。
- 此结论仅覆盖七个远端分支的可达历史。GitHub 隐藏 PR 引用、缓存和他人既有克隆不在本次清理范围；旧克隆须同步重写历史，避免重新推回旧提交。旧历史备份与审计记录仅保存在仓库外。

## 2026-09-24 — 分发配置与学员隐私核对

- 已上传的 1.0（33）归档包含本机发布配置指定的 AI relay 地址，新安装通过自动生成安装身份获取授权，无需用户填写供应商密钥。归档中未发现数据库、工作簿、互传包或测试 fixture；动作种子 clients 为空，usage counts 为零，已知学员身份扫描无匹配。
- Git 当前内容使用不可连接的示例地址，不包含实际 relay 主机名或已知学员身份。通用 AI 接入代码及自建 Worker 示例保留，使用者需自行部署配置。真实地址保存在忽略的 Local.xcconfig，供应商密钥保存在云端 Secrets。
- 对远端各分支历史的额外检查发现旧提交的两处源码注释包含已知学员身份，共涉及六个历史文件版本；当前 main 已无这些姓名。历史清理需重写受影响的远端分支，尚未执行，不能宣称全部 Git 历史已脱敏。
- 增加仓库检查：拒绝真实 workers.dev 主机名与非占位共享配置；发布归档检查拒绝数据库/导出文件、测试种子及动作库学员数据。已对实际上传归档和当前源码运行通过；身份词表及审计证据仅保存在仓库外。

## 2026-09-24 — 1.0（33）Git 同步与 TestFlight 上传

- 审阅修复、回归测试、构建号及 Worker 部署记录已提交为 `33fcae0`，并连同此前待推送的本地提交推送至 GitHub `main`。
- 使用当前 Release 配置生成独立 Archive；App 与 GymLogKit 均为 1.0（33）。codesign 严格校验和发布隐私检查通过（2 个 bundle，含麦克风说明，无 Apple Speech 依赖）。此前同构建号的真机安装与数据保留验证见下文。
- 香港时间 14:27:53，Apple 返回 `Uploaded package is processing`、`Upload succeeded` 与 `EXPORT SUCCEEDED`。上传包和日志位于仓库外，未纳入 Git。
- 已完成 TestFlight 上传，Apple 后处理及可下载状态尚未核验；App Store Connect 网页无有效登录会话。GitHub 源码提交的 repository、worker 检查已通过，记录时 ios CI 仍在执行。

## 2026-09-24 — Worker 审阅修复上线

- 已部署 `gymlog-cloud-relay`，新版本 `880368a4-ff9d-4da0-a391-8793eba51e2d`，替换 `1feab0dd-15c5-4bf2-87d4-b26b9dfcf52f`。本次上线个人额度与全局预算串行接纳、错误分类与 Retry-After、冲突 token 上限及非对象 JSON 拒绝等修复；沿用现有密钥、DO 存储、prompt 白名单及每日额度。
- 部署前 TypeScript 检查及 20 项后端测试全部通过。线上 health 200、无授权 usage 401、测试身份领取授权 200；双 token 上限、非对象 JSON、未登记 prompt 均返回 400，个人 used 保持 0。
- 使用合成指令“开始今天的训练”和 App 实际固定 prompt 调用真实模型：HTTP 200，SSE 正常完成，返回 version 1 / startSession，evidence 与原输入一致。重放同一授权返回 409，最终 used 为 1；未提交真实学员或训练资料。
- 首次 Python HTTP 健康检查返回 403，改用 curl 后健康及以上检查通过；该单次客户端差异原因未确定。未在线耗尽全局预算或触发 IP 限流，这两项依靠已有本地运行时/自动化验证；真实录音 ASR 与手机端完整交互仍待实测。已安装的 1.0（33）沿用同一服务地址，无需重新安装。
- 回滚命令：`npx wrangler rollback 1feab0dd-15c5-4bf2-87d4-b26b9dfcf52f`。验证摘要保存在仓库外；未提交、推送或上传 TestFlight。

## 2026-09-24 — 1.0（33）真机安装

- 将当前已验证的审阅修复构建为 Release 1.0（33），App 与 GymLogKit 构建号一致；codesign 严格校验及发布隐私检查通过。
- 覆盖安装到用户 iPhone Air，沿用既有应用身份。安装前终止旧进程并备份 Application Support 与 Preferences；未发现进行中的磁盘草稿。整份 Library 复制曾因系统 SplashBoard 快照权限失败，改为分别备份业务数据和偏好设置，均成功。
- 安装后确认版本为 33；数据库 integrity_check 为 ok，11 张业务表逐行摘要与安装前一致。首次启动前和成功启动后的检查均一致。
- 首次启动请求因锁屏被拒绝；解锁后重试成功，并确认新 App 进程存在。已完成安装与启动验证，尚不代表全部真机交互验收。Worker 后端未部署，未上传 TestFlight、未提交或推送。

## 2026-09-24 — 工程审阅修复（本地验证，未部署）

- 修复 R1–R4 / R8：重新打开同一课次只导航、不覆盖当前草稿；快速编辑只提交修改字段并保留小数精度及原文；互传包在解析和直接提交入口拒绝重复/空动作 ID；历史 unknown actual 原文通过草稿、快照、拆组和保存保留，明确清除与复制新课次仍清空实际；语音调整组数/组成 Superset 保留已有组的来源标记。
- `SessionEditingCoordinator` 移入 GymLogKit，直接测试生产代码的重新打开行为；未变更 SwiftData 数据库 schema，新快照字段为可选。
- 修复 R5–R7：安装 DO 串行校验个人资格和全局预算，通过后才保存个人扣减；全局拒绝/不可用不消耗个人额度，重复操作不占全局槽位。区分 IP 限流、操作数限制、月额度、日预算与过期操作；携带 Retry-After 并匹配客户端提示。拒绝同时提交两个 token 上限以及非对象 JSON。
- AI 评价上下文补入有来源标记的未受信任训练备注；生成期间同时检查完整输入上下文，历史变化时拒绝保存过期回复。已保存评价仍按当前课次/目标/语言/备注决定是否过期。
- 后端新增部署 prompt 白名单与 Swift prompt 测试常量之间的一致性检查；不修改 prompt 或线上白名单。
- 验证：698 项 iOS 单元测试通过，模拟器 App build 成功；2 项 UI 冒烟（Superset 组合、云端文字计划及整句撤销）通过；TypeScript 与 20 项后端测试通过。仓库检查和 diff whitespace 检查通过；本机无私密词表文件，未做 Git 全历史扫描。
- Wrangler 本地运行时使用合成身份和 MOCK_UPSTREAM 验证：同授权 4 路并发仅 1 个 200，其余 409；全局限额拒绝返回 503，个人 used 保持 1；同一被拒绝操作再次尝试仍为预算拒绝而非已消费。无真实供应商调用。
- 边界：全局接纳之后如个人 DO 写存储失败，全局槽位可能保守计入；不会突破全局上限。未部署 Worker、未真机安装、未上传 TestFlight、未提交或推送。未执行完整 UI 套件、真机 BLE/录音和旧磁盘库迁移验证。分页与统计性能优化留作单独工作。

## 2026-09-23 — Worker 加固上线

- 部署 `gymlog-cloud-relay` 版本 1feab0dd（取代 2026-09-14 的 71a73107，该版本早于公开仓库首个 commit）。密钥未改动；新增每日额度（ASR 2000 / 文字理解 3000）、授权发放按 IP 限流（20 次/分钟）、清理接口 prompt 指纹白名单。
- 线上检查：/healthz 200；无令牌 401；随机测试身份可领授权；未登记的 system prompt 返回 400 且未计额度。未发起真实供应商调用，语音与 AI 评价待用户在 1.0（32）上实测。
- 回滚：`npx wrangler rollback 71a73107-103a-4b62-bd2a-75022b418d2a`。2026-09-15 前构建的 App 因 prompt 指纹不符将被拒绝。

## 2026-09-23 — 1.0（32）真机安装

- 源码 75baf49 加构建号 31 → 32；Release 签名构建，codesign 严格校验通过，App 与 GymLogKit 均为 32，服务地址为现有 Worker（本次 Worker 改动未部署）。
- 覆盖安装至用户 iPhone Air，沿用原应用身份。安装前 App 未运行、无进行中草稿；已备份数据库与偏好设置（仓库外忽略目录）。
- 安装后数据库完整性 ok，11 张业务表逐行内容与安装前一致。
- 待用户真机使用验收；未上传 TestFlight，未推送 GitHub。

## 2026-09-23 — 审阅修复（未发版）

### 变更范围
- 从历史「完整编辑」后保存，不再清空块备注、休息原文、原始动作名与推断组标记；未记录的时长保持未记录；含本版本无法解码 WOD 的课次拒绝完整编辑。
- 删除注释中的学员姓名，模板来源如实标注；取消跟踪同步冲突副本与 `.claude/launch.json`；仓库检查新增冲突副本、Team ID 与本地私人词表检测。
- Worker 新增全局每日预算、按 IP 限制授权发放、清理接口只接受应用请求形状与固定 system prompt 指纹。
- 未推送的 4 个 commit 已改写，移除重复文件与本地配置；改写前的引用保留在本地 `refs/original/`。

### 验证与边界
- 690 项单元测试通过（2 项跳过），App 构建成功；Worker 13 项测试通过，`wrangler deploy --dry-run` 配置校验通过。
- 未上真机，Worker 未部署。学员姓名仍存在于已推送的 GitHub 历史中。
- UI 测试：首次全量 30 项中 8 项失败，在改动前的 c2f185b 上同样失败，属于界面改版后未更新的旧测试。已按现行界面更新（Superset 先经模板库、语音入口仅在「今天」、悬浮标签栏遮挡、体测展开后懒加载），并修正课次详情块标识覆盖子元素标识的问题，以及能耗卡片日期不随应用语言的问题。更新后 30 项全部通过，单元测试 690 项通过。

## 2026-09-22 — 1.0（31）

### 变更范围
- 将当前工作区已完成的个人档案、历史、动作库及训练记录界面调整一并纳入版本管理；包含中英文动作名层级、个人资料分组编辑、历史展开、今日页语音入口及主题预览可访问性改进。
- Excel 表头兼容 Weight / Weights、大小写与多余空白；跨训练工作表按 Week 编号稳定合并后重建日期，修复工作表顺序造成的日期回退。仍拒绝错列及不合法数据。
- 实际成绩选择器点击“完成”会确认当前默认值，不再要求先滚动；清除操作继续保留未记录状态。
- 无 InBody 记录时改为独立引导卡片，显示测量用途、扫描主按钮与手动新增入口，隐藏无意义的“历史记录 · 0”。有数据时保留原有概览、趋势和历史入口。
- 工程版本源为 GymLog/project.yml，已重新生成 Xcode 工程。生成工程和本地签名配置仍按现有规则排除在 Git 外。
- 用户要求将目前所有改动存入本地 Git，不推送 GitHub。设计资料、参考源码及现有重复文件作为工作区快照保留，不参与应用 target 编译。

### 验证与边界
- Build 30：685 项单元测试和 2 项重量/实际成绩界面测试通过。用户提供的工作簿完整解析为 85 个课次、138 个动作；原文件未修改，也未纳入仓库。
- Build 30 已覆盖安装并启动；更新前后数据库完整性均正常，课次、组记录和身体指标数量一致。用户随后确认测试完毕。
- Build 31：空白状态模拟器构建及截图检查通过，扫描与手动新增按钮可访问。检查中发现父容器可访问性标识覆盖子按钮标识，已移除多余父标识并重新验证。临时截图检查方法未保留为正式测试。
- Build 31 尚未获得用户真机验收；不可将 Build 30 的验收视为对新空白状态的验收。
- 发布凭证、数据库备份及截图存放在仓库外，签名凭据未加入 Git。上传与 Apple 后处理状态分开记录。

### 发布状态
- 源码与全部工作区资料已提交：8cac403（2026-09-23 为移除重复文件与本地配置改写前为 131aed6）；未推送 GitHub。
- Build 31 最终 Release 归档成功，App 与 GymLogKit 均为 1.0（31）；签名和二进制隐私检查通过。
- 已覆盖安装到用户 iPhone Air，并成功启动；设备应用列表确认版本 1.0（31）。新空白状态尚待用户实际体验。
- 2026-09-22 22:45 香港时间，Apple 返回 “Uploaded package is processing”、 “Upload succeeded” 和 “EXPORT SUCCEEDED”。这是上传成功及处理中的凭证，不代表 TestFlight 已可下载。
- 仓库外发布目录：Developer/Artifacts/GymLog/release-build31；保留最终归档、上传日志与空白状态截图。

# 工程记录

## 2026-09-23 — Worker 加固上线

- 部署 `gymlog-cloud-relay` 版本 1feab0dd（取代 2026-09-14 的 71a73107，该版本早于公开仓库首个 commit）。密钥未改动；新增每日额度（ASR 2000 / 文字理解 3000）、授权发放按 IP 限流（20 次/分钟）、清理接口 prompt 指纹白名单。
- 线上检查：/healthz 200；无令牌 401；随机测试身份可领授权；未登记的 system prompt 返回 400 且未计额度。未发起真实供应商调用，语音与 AI 评价待用户在 1.0（32）上实测。
- 回滚：`npx wrangler rollback 71a73107-103a-4b62-bd2a-75022b418d2a`。2026-09-15 前构建的 App 因 prompt 指纹不符将被拒绝。

## 2026-09-23 — 1.0（32）真机安装

- 源码 75baf49 加构建号 31 → 32；Release 签名构建，codesign 严格校验通过，App 与 GymLogKit 均为 32，服务地址为现有 Worker（本次 Worker 改动未部署）。
- 覆盖安装至用户 iPhone Air，沿用原应用身份。安装前 App 未运行、无进行中草稿；已备份数据库与偏好设置（仓库外忽略目录）。
- 安装后数据库完整性 ok，11 张业务表逐行内容与安装前一致（学员 4、课次 215、组记录 3980、体测 12）。
- 待用户真机使用验收；未上传 TestFlight，未推送 GitHub。

## 2026-09-23 — 审阅修复（未发版）

### 变更范围
- 从历史「完整编辑」后保存，不再清空块备注、休息原文、原始动作名与推断组标记；未记录的时长保持未记录；含本版本无法解码 WOD 的课次拒绝完整编辑。
- 删除注释中的学员姓名，模板来源如实标注；取消跟踪同步冲突副本与 `.claude/launch.json`；仓库检查新增冲突副本、Team ID 与本地私人词表检测。
- Worker 新增全局每日预算、按 IP 限制授权发放、清理接口只接受应用请求形状与固定 system prompt 指纹。
- 未推送的 4 个 commit 已改写，移除重复文件与本地配置；改写前的引用保留在本地 `refs/original/`。

### 验证与边界
- 690 项单元测试通过（2 项跳过），App 构建成功；Worker 13 项测试通过，`wrangler deploy --dry-run` 配置校验通过。
- 未上真机，Worker 未部署。学员姓名仍存在于已推送的 GitHub 历史中。
- UI 测试：首次全量 30 项中 8 项失败，在改动前的 c2f185b 上同样失败，属于界面改版后未更新的旧测试。已按现行界面更新（Superset 先经模板库、语音入口仅在「今天」、悬浮标签栏遮挡、体测展开后懒加载），并修正课次详情块标识覆盖子元素标识的问题，以及能耗卡片日期不随应用语言的问题。更新后 30 项全部通过，单元测试 690 项通过。

## 2026-09-22 — 1.0（31）

### 变更范围
- 将当前工作区已完成的个人档案、历史、动作库及训练记录界面调整一并纳入版本管理；包含中英文动作名层级、个人资料分组编辑、历史展开、今日页语音入口及主题预览可访问性改进。
- Excel 表头兼容 Weight / Weights、大小写与多余空白；跨训练工作表按 Week 编号稳定合并后重建日期，修复工作表顺序造成的日期回退。仍拒绝错列及不合法数据。
- 实际成绩选择器点击“完成”会确认当前默认值，不再要求先滚动；清除操作继续保留未记录状态。
- 无 InBody 记录时改为独立引导卡片，显示测量用途、扫描主按钮与手动新增入口，隐藏无意义的“历史记录 · 0”。有数据时保留原有概览、趋势和历史入口。
- 工程版本源为 GymLog/project.yml，已重新生成 Xcode 工程。生成工程和本地签名配置仍按现有规则排除在 Git 外。
- 用户要求将目前所有改动存入本地 Git，不推送 GitHub。设计资料、参考源码及现有重复文件作为工作区快照保留，不参与应用 target 编译。

### 验证与边界
- Build 30：685 项单元测试和 2 项重量/实际成绩界面测试通过。用户提供的工作簿完整解析为 85 个课次、138 个动作；原文件未修改，也未纳入仓库。
- Build 30 已覆盖安装并启动；更新前后数据库完整性均正常，课次、组记录和身体指标数量一致。用户随后确认测试完毕。
- Build 31：空白状态模拟器构建及截图检查通过，扫描与手动新增按钮可访问。检查中发现父容器可访问性标识覆盖子按钮标识，已移除多余父标识并重新验证。临时截图检查方法未保留为正式测试。
- Build 31 尚未获得用户真机验收；不可将 Build 30 的验收视为对新空白状态的验收。
- 发布凭证、数据库备份及截图存放在仓库外，签名凭据未加入 Git。上传与 Apple 后处理状态分开记录。

### 发布状态
- 源码与全部工作区资料已提交：8cac403（2026-09-23 为移除重复文件与本地配置改写前为 131aed6）；未推送 GitHub。
- Build 31 最终 Release 归档成功，App 与 GymLogKit 均为 1.0（31）；签名和二进制隐私检查通过。
- 已覆盖安装到用户 iPhone Air，并成功启动；设备应用列表确认版本 1.0（31）。新空白状态尚待用户实际体验。
- 2026-09-22 22:45 香港时间，Apple 返回 “Uploaded package is processing”、 “Upload succeeded” 和 “EXPORT SUCCEEDED”。这是上传成功及处理中的凭证，不代表 TestFlight 已可下载。
- 仓库外发布目录：Developer/Artifacts/GymLog/release-build31；保留最终归档、上传日志与空白状态截图。

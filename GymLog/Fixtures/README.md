# Synthetic fixture

`sample_seed.json` is a hand-written synthetic test fixture. Its athlete labels, sessions and values are examples, not real people or measurements. It is bundled into the test target only.

Do not add real workbooks, body-composition photos, OCR transcripts, database exports or personal history here. Tests must be self-contained and use synthetic data. The repository privacy gate rejects unreviewed fixtures.
